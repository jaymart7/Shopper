CREATE TABLE Token(
  id TEXT NOT NULL PRIMARY KEY,
  token TEXT NOT NULL
);

insertItem:
INSERT OR REPLACE INTO Token(id, token) VALUES(1,?);

selectAll:
SELECT * FROM Token LIMIT 1;

deleteAll:
DELETE FROM Token;